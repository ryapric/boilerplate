"""
This is a main module file. In it, you should:

- `export` the desired functions from your `include()` calls (comma-delimited)

- `include()` your `srcfile`s
"""

module PkgName

export sharpe

include("./srcfile.jl")

end # module PkgName
